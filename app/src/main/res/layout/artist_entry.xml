<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="100dp">

    <ImageView
        android:id="@+id/artist_image_preview"
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:contentDescription="@string/cover_thumb_content_description"
        android:padding="5dp"
        tools:srcCompat="@tools:sample/avatars" />

    <RelativeLayout
        android:id="@+id/rest_of_artist_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">

        <TextView
            android:id="@+id/artist_name_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:padding="3dp"
            android:text="@string/default_release_title"
            android:textColor="@android:color/black"
            android:typeface="monospace" />

    </RelativeLayout>

</LinearLayout>
